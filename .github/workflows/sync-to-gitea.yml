name: Sync to Gitea
on:
  push:
    branches:
      - main  # Cambia a 'master' si es tu rama principal
  pull_request:
    types: [closed]  # Sincroniza al mergear PRs
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Trae todo el historial
      - name: Push to Gitea
        env:
          GITEA_TOKEN: ${{ secrets.GITEA_TOKEN }}
        run: |
          git remote add gitea https://jaumesastre:${GITEA_TOKEN}@github.jaumesastre.es/jaumesastre/Dotfiles-ArchLinux.git || true
          git push --mirror gitea
